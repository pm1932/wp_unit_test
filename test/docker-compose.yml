version: '2'
services:
  web:
    build:
      context: ./phpunit/
    ports:
      - 8083:80
    volumes:
      - /Users/no113/PhpstormProjects/ProtoTyping/app:/var/www/html:cached
      - ./phpunit/php.ini:/usr/local/etc/php/php.ini:cached
    links:
      - db:mysql
    environment:
      HOST_PORT: 8083
      HOST_NAME: cs2_0.pm1932.jp
      XDEBUG_CONFIG: remote_host=192.168.1.66
      PHP_IDE_CONFIG: "serverName=my.local"

  db:
    build:
      context: ./mysql/
    ports:
      - 8084:3306
    volumes:
      - ./mysql/my.cnf:/etc/mysql/conf.d/my.cnf:cached
      - ./mysql/init.d:/docker-entrypoint-initdb.d:cached
    environment:
      MYSQL_ROOT_PASSWORD: pman7170
      MYSQL_DATABASE: wp_db
